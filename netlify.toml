# Netlify config so the /api/* serverless functions (Epic login + cloud sync) deploy.
# The static app still lives at the repo root and works on its own without any of this.
[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
